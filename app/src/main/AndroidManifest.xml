<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.hhw.project_with_a_jump">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> // 위치 관련 권한
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_with_a_jump">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CertifyActivity"> </activity>
        <activity android:name=".CertifyActivity2"> </activity>
        <activity android:name=".EntryActivity"> </activity>


    </application>

</manifest>